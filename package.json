{
  "name": "awesome-mini-game-desktop",
  "version": "0.1.4",
  "private": true,
  "description": "Awesome Mini Game macOS 桌面版壳 (Tauri v2)，网页本体零改造",
  "scripts": {
    "sync-web": "python3 scripts/sync-web.py",
    "tauri:dev": "python3 scripts/sync-web.py && (lsof -iTCP:8000 -sTCP:LISTEN >/dev/null 2>&1 || (python3 -m http.server 8000 >/tmp/amg-dev-server.log 2>&1 &)) && tauri dev",
    "tauri:build": "python3 scripts/sync-web.py && tauri build --bundles app,dmg",
    "tauri:icon": "tauri icon",
    "tauri:signer": "tauri signer generate"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
